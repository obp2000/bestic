= error_messages_for :mugshot

- form_for(:mugshot, :url => mugshots_path, :html => { :multipart => true }) do |f|
  %p
    %label( for="mugshot" ) Upload A Mugshot:
    = f.file_field :uploaded_data
  %p
    = submit_tag 'Create'
